---
description: 默认协作方式：可代劳的由 Agent 完成
alwaysApply: true
---

# 默认协作方式

- 默认由 Agent 端到端处理任务：能由 Agent 完成的步骤，直接执行并验证结果。
- 仅在确实需要用户参与时再请用户操作（例如第三方登录授权、验证码、支付确认、平台权限限制）。
- 需要用户介入时，先明确原因，再给最短可执行步骤；用户完成后由 Agent 继续收尾。
- 未被明确要求时，不把任务拆成“你去做 + 我等待”的形式。
